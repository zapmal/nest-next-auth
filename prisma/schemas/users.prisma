
model Users {
  id Int @default(autoincrement()) @id
  email String @unique @db.VarChar(125)
  name String? @default('No name provided') @db.VarChar(125)
  password String @db.VarChar(255)
  refresh_token String?
}
